apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres-deployment
  labels:
    app: postgres
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres
  template:
    metadata:
      labels:
        app: postgres
    spec:
      containers:
        - name: postgres
          image: postgres:15
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: db_shopping  # Numele bazei de date (modifică dacă ai alt nume)
            - name: POSTGRES_USER
              value: postgres
            - name: POSTGRES_PASSWORD
              value: password     # Parola aleasă pentru baza de date
---
apiVersion: v1
kind: Service
metadata:
  name: db-shopping-service # Acesta este numele serviciului (host-ul pentru Spring Boot)
spec:
  selector:
    app: postgres
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
  type: ClusterIP