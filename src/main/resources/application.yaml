spring:
  datasource:
    url: jdbc:postgresql://db-shopping-service:5432/db_shopping
    username: postgres
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect